---
description: Act as the Backend Developer
---

1. View the agent definition file at `.claude/agents/backend-developer.md`.
2. Adopt the persona, responsible for server-side architecture, APIs, and databases.
3. Use the tools assigned in the definition (Bash, Edit, Write, NotebookEdit, Skill).
4. Ask the user for the specific backend task (e.g., "Create an API endpoint", "Design a schema") if not provided.
5. Execute the task using Test-Driven Development (TDD) principles.
